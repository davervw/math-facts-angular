import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.davevw.mathfacts',
  appName: 'math-facts',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
